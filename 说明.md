# g-memtest 项目说明

更新时间：2026-09-21

## 这是什么

中文自测刷题站。内置题库在 `site/banks/`，练习进度存在浏览器 `localStorage`。本地用 `npm start` 可以写入、覆盖、删除内置题库；线上是纯静态站，只能刷题和导入。

- 本地项目：`E:\Cherry Studio\agent_4\g-memtest`
- GitHub：`https://github.com/gyj0415/g-memtest`
- 线上：`https://g-memtest.netlify.app/`
- 发布：Netlify 已接 GitHub，推 `main` 会自动发布 `site/`。不要用 GitHub Pages。

日常怎么用，看根目录 `README.md`。

## 三个页面

| 页面 | 地址 | 做什么 |
| --- | --- | --- |
| 自测网站 | `site/index.html` | 选题库、刷题、导入 JSON/ZIP |
| 题库转换器 | `site/converter.html` | 把 md 转成题库；线上下载 ZIP；本地也可写入内置 |
| 制作题库 | `site/studio.html` | 只在 `npm start` 时可用：写入 / 删除内置 |

本地启动：

```powershell
npm start
```

打开 [http://127.0.0.1:4173/](http://127.0.0.1:4173/)

## 目录

```text
g-memtest/
├─ site/                 # Netlify 发布这一层
│  ├─ index.html         # 自测网站
│  ├─ converter.html     # 转换器
│  ├─ studio.html        # 本地制作页
│  ├─ banks/             # 内置题库 JSON + manifest.json
│  └─ images/            # 内置题库图片，按题库 id 分子目录
├─ scripts/
│  ├─ dev-server.mjs     # npm start
│  └─ lib/               # 写入/删除内置、打包 zip
├─ tests/
├─ README.md             # 快速使用
└─ 说明.md
```

## 内置题库

首页按 `group` 折叠。当前分类大致是：企业管理、毛概、毛概2、技能训练、公务员考试。清单以 `site/banks/manifest.json` 为准。

本地写入后会改 `site/banks/` 和 `manifest.json`，有图则放到 `site/images/<题库id>/`。删内置题库时，对应图片目录也会删。

自测网站加载内置清单时不走缓存；删掉的内置不会继续留在首页。

## 题库从哪来

1. **内置**：`site/banks/`，所有人打开网站都能看到（上线后）。
2. **本地导入**：当前浏览器的「本地题库」，JSON 或 ZIP 都可以。
3. **云端候选**：也是存在当前浏览器，方便自己分类；纯静态站不能真的传到服务器。

练习记录按题库分开存，换题库不会把另一套的进度清掉。

题目文本格式、配图步骤见 `README.md`。带图请用 ZIP（JSON + 图片），不要把图片嵌进单个 JSON。

## 自测页注意点

- 题目图片默认偏小，右侧滑条可调，调过会记住。
- 选择题：点选项只选中，Enter 才判分并下一题。
- 不要改首页题库卡片的桌面布局。

## 检查和上线

```powershell
npm test
npm run typecheck
npm run lint
```

上线：把改动推到 `main` 即可。Netlify 会发布 `site/`。

## 注意

- 中文界面可以；仓库文件名尽量简单。
- 不要把课程原始资料、下载目录、临时文件提交进仓库。
- 改了页面或内置题库，推到 `main` 后等 Netlify 部署完成，线上才会更新。
